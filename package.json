{
  "name": "super-chat",
  "version": "1.0.0",
  "description": "real time chat app",
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "npm run build -w frontend && npm run generate -w backend && npm run build -w backend",
    "start": "npm run migrate:deploy -w backend && npm run start -w backend"
  },
  "engines": {
    "node": "24.12.0"
  },
  "workspaces": [
    "backend",
    "frontend",
    "shared"
  ],
  "dependencies": {
    "browser-image-compression": "^2.0.2"
  }
}